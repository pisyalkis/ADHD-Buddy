# Баги и риски

## 2026-08-26

- Последовательный проход по задачам (`walk_tasks_start` → `_walk_to_step`, bot.py:4630-4695) ставит `ctx.user_data["task_walk"] = True`, но если в пуле дел уже что-то есть, `_offer_task_input` показывает клавиатуру с кнопками `pool_use_item`/`pool_write_own` (bot.py:4971-4981, 4772-4781) — обе первым делом вызывают `clear_awaiting_flags(ctx, update)`, который безусловно удаляет `task_walk` (bot.py:5350) ещё до того, как `apply_task_edit` (bot.py:4961) успевает его проверить. В результате проход "по одной" молча обрывается на первом же шаге, где пул не пуст (частый случай), и пользователя вместо следующего слота возвращает на полный список задач.
- `apply_task_edit` (bot.py:4912-4969) снимает отметку "выполнено" с задачи при любом повторном сохранении слота, даже если новый текст совпадает со старым — `was_filled` проверяет только факт непустоты, а не изменение текста, хотя комментарий рядом (bot.py:4949-4951) описывает намерение снимать отметку именно при *смене* текста. Повторное сохранение того же текста (например через "✏️ Поменять" без реальной правки) откатывает уже выполненную задачу обратно в невыполненную.
- `finish_morning`/`_build_pinned_tasks_text` (bot.py:2298, 2503): `g(user['gender'], 'записано', 'записана')` согласует причастие с полом пользователя, а не с родом существительного "Утро" (средний род, всегда "записано"). Пользовательницы (gender='F') видят "✅ *Утро записана!*" — грамматическая ошибка, дублирующаяся теперь в двух местах (обычный текст сообщения и билдер закреплённого сообщения с зачёркиванием). Fallback-ветка при исключении использует правильный инвариантный текст "Утро записано!", что подтверждает: основной путь сломан, это не осознанный выбор.
- `personalize("😵 Отвлёкся(ась)", gender)` (bot.py:3561) не работает ни для одного пользователя: словарь неправильных форм `_PERSONALIZE_IRREGULAR` (bot.py:1027-1034) хранит ключ в нижнем регистре `"отвлёкся(ась)"`, а текст кнопки — с заглавной "Отвлёкся(ась)"; `str.replace` регистрозависим, общий regex-фоллбэк тоже не подходит (ждёт `(ла)`/`(а)`, а не `(ась)`). Кнопка коуча всегда показывает неперсонализированный текст "😵 Отвлёкся(ась)" для всех полов.
- `create_promo_code` (bot.py:636-645) делает `INSERT OR REPLACE`, который безусловно обнуляет `uses` при повторном создании кода с тем же именем, хотя история использований в `promo_redemptions` не удаляется, а лимит в `redeem_promo_code` проверяется только по счётчику `uses`. Если админ пересоздаёт уже использованный лимитированный промокод (например, чтобы поправить лейбл), лимит редемпшенов эффективно снимается и открывается заново.
- `check_notifications` (bot.py:7353-7381): `tz`, `now_dt`, `now`, `is_monday` и `day_key` вычисляются из `user`, взятого из общего снимка `get_all_notif_users()` в начале тика — и только *после* этого код перечитывает `user = get_user(uid)` свежим, чтобы закрыть уже известный баг с устаревшим `notif_enabled` (см. комментарий 16-го чекапа рядом). Однако сам часовой пояс и производные от него значения времени/даты выше по коду так и остаются вычисленными по старому снимку. Если пользователь меняет город/таймзону в тот момент, когда тик как раз перебирает пользователей, для этого тика могут сработать утренние/дневные/вечерние/недельные проверки по неверному локальному времени и календарной дате.
- `send_due_reminders` (bot.py:5205-5224): `rem` берётся один раз до `await app.bot.send_message(...)`; если пользователь успевает отредактировать это же напоминание (текст/время/повтор) во время этого `await`, последующий `reschedule_reminder`/`cancel_reminder` всё равно использует старые `rem["recur"]`/`rem["remind_at"]`, затирая только что сделанную правку — например, отключение повтора прямо в момент срабатывания не помешает напоминанию снова перепланироваться как повторяющееся.
- `_edit_or_send` (bot.py:1363-1373) — новый общий хелпер для "12 разделов бота теперь редактируют то же сообщение вместо нового" (#146): ловит вообще любое исключение из `q.message.edit_text(...)` и на любой ошибке шлёт `reply_text` тем же текстом как новое сообщение. Telegram Bot API кидает `BadRequest: Message is not modified`, когда текст и клавиатура совпадают с уже показанными — а это ровно то, что происходит при повторном/двойном тапе по одной и той же кнопке статичного экрана (🤖 Коуч, ℹ️ О боте, 🔒 Приватность, 🧠 Навыки, ⏰ Напоминания, 🗂 Карточка дня, 🍅 Фокус-режим, 💬 Обратная связь, 💎 Подписка, 🆕 Что нового, буддi, вкладки ⚙️ Общие и т.д. — все они теперь используют `_edit_or_send`, bot.py: строки со звонками ~3794, 3829, 3926, 3934, 4104, 4279, 4289, 4869, 5415, 5604, 6028, 6425, 6477, 6479, 6527, 6576, 6914). Двойной тап (а импульсивный повторный тап — типичное поведение именно для аудитории СДВГ, на которую рассчитан бот) не различается как "то же самое", падает в `except`, и вместо no-op редактирования уходит дублирующее новое сообщение — то есть именно тот чат-спам, который вся эта серия коммитов должна была устранить, тут же возвращается для самого частого сценария. `go_tab` (bot.py:5670-5684) содержит отдельную неразделяемую копию того же самого паттерна `try: edit_text / except: reply_text` — тот же баг, но фикс в `_edit_or_send` его не затронет, пока копия не приведена к общей функции.

## 2026-08-27

- `send_tracked_notification` (bot.py:2241-2280) поддерживает `ttl_seconds=0` специально для случая "самоудаление по тишине было бы багом, а не уборкой" — с явным комментарием в коде, что это нужно фокус-таймеру, пока раунд идёт. Но ни один из реальных вызовов канала `"focus_timer"` этот параметр не передаёт (bot.py:6706-6707 "Таймер уже идёт!", 6729-6730 старт таймера, 6794-6795 "Таймер запущен"), поэтому используется дефолт `INACTIVE_SCREEN_TTL_SEC = 900` (bot.py:4455-4456) — 15 минут. Раунды длительностью 25/30/45/60/90 минут — обычное намеренное молчание в фокус-режиме — приводят к тому, что сообщение со статусом таймера и кнопкой "⏹ Остановить" исчезает из чата за 10-75 минут до конца раунда, хотя таймер продолжает идти.
- Маячок навыков с анимацией (дыхание/заземление) шлёт GIF отдельным, неотслеживаемым сообщением через `app.bot.send_animation` (bot.py:5235) перед текстом-приглашением, который отправляется через `send_tracked_notification` с каналом `"skill_beacon"` (bot.py:5241-5249) и поэтому корректно заменяет предыдущий текст того же канала. У анимации аналогичной логики нет — при каждом новом срабатывании маячка с той же техникой в чате копится ещё одна GIF, то есть именно то "накопление сообщений одно за другим", которое вся серия правок "одно актуальное сообщение" должна была устранить, только для анимаций фикс не применили.
- `why_callback` (bot.py:1315-1329) при нажатии "❓ Зачем это?" на маячке навыков (`why_beacon_technique`) сохраняет id объяснения в `ctx.user_data["why_msg_beacon_technique"]`, а `_cleanup_why_msg(ctx, uid, "beacon_technique")` (bot.py:1331-1339) вызывается только из `beacon_technique_done` (bot.py:5032) — единственного способа явно ответить на маячок. Если пользователь открыл объяснение, но проигнорировал сам маячок (обычный сценарий для периодической фоновой нотификации), а сообщение маячка потом самоудалится по тишине (`send_tracked_notification` с дефолтным TTL для канала `skill_beacon`), объяснение остаётся висеть в чате навсегда — прямо вопреки цели, заявленной в докстринге `why_callback`: "оно не должно оставаться в чате после того, как поле уже заполнено".
- Онбординг `got_name` (bot.py:1776-1784) проверяет только `len(name) > 30`, но не пустую строку — в отличие от аналогичного экрана смены имени в Настройках, который явно отклоняет пустое имя (`"Имя не может быть пустым"`, bot.py:6397-6399). Сообщение, которое после `.strip()` даёт `name=""` (например, из одних пробелов), сохранится в БД как есть. `get_all_notif_users()` фильтрует `WHERE name!=''` (bot.py:853), так что такой пользователь никогда не попадёт в уведомления, а `start()` (bot.py:1745, `if user["name"]:`) будет считать его незарегистрированным и при каждом `/start` заново показывать онбординг с нуля.
- Все места, где бот обращается к Anthropic (`parse_reminder_request` bot.py:3969-3973, `classify_free_text` bot.py:4021-4025, а также `ai_morning_boost`/`ai_day_analysis`/`send_coach`) создают синхронный `Anthropic(api_key=ANTHROPIC_KEY)` и вызывают `client.messages.create(...)` без `await`/`asyncio.to_thread` внутри `async def`-хендлеров. Пока идёт сетевой запрос к Claude, единственный event loop бота полностью блокируется — не обрабатываются апдейты других пользователей и не тикает раз-в-минутный `check_notifications` (bot.py:8508+), от которого зависят все напоминания и маячки.
- `_edit_msg_or_send` (bot.py:1399-1408) — общий примитив, лежащий в основе `_edit_or_send`, `_render_tracked` и большинства "редактируемых на месте" экранов (в т.ч. списка дел, bot.py:5937, 5942) — ловит исключение только вокруг `message.edit_text(...)` и в `except`-ветке безусловно вызывает `message.reply_text(...)` без своего try/except. Если итоговый текст превышает лимит Telegram в 4096 символов (а длина пользовательского текста задачи/пункта списка дел нигде не ограничивается), `edit_text` упадёт по этой причине и попадёт в `except`, но `reply_text` с тем же текстом упадёт с тем же самым исключением уже без перехвата — оно всплывёт наружу и уронит вызывающий хендлер.

## 2026-08-28

- `admin_feedback`/`admin_research` (bot.py:8830-8850, 8856-8901) режут собранное Markdown-сообщение на куски по сырому числу символов — `for i in range(0, len(msg), 4000): await update.message.reply_text(msg[i:i+4000], parse_mode="Markdown")`. Записи внутри `msg` оформлены как `*{name}* (...):\n{text}` с непарными `*`; если граница 4000-символьного среза попадает внутрь такой пары звёздочек, тот конкретный `reply_text` падает с `BadRequest: Can't parse entities`. Исключение внутри команды ничем не перехвачено, `on_error` его только логирует — админ получает часть фидбеков/ответов на исследования без предупреждения, а "хвост" списка молча теряется. Это отдельный сценарий от уже задокументированного бага `_edit_msg_or_send` (там — одно сообщение длиннее 4096 и зависший диалог, а не порча разметки при принудительной нарезке на несколько сообщений).
- `handle_text`, ветка `awaiting_buddy` (bot.py:6484-6491) не проверяет пустую строку после `strip()`, в отличие от соседней ветки `awaiting_name` (bot.py:6465-6472) буквально на 19 строк выше. Сообщение из одних пробелов сохранится как `buddy_name=""` и бот тут же бодро подтвердит "👥 *Бадди добавлен: *" (пустое имя внутри жирного выделения) вместо просьбы повторить ввод — а `buddy_menu` после этого снова покажет "Бадди не задан", потому что пустая строка falsy.
- `toggle_beacon_type_callback` (bot.py:4778-4791) позволяет снять галочки со всех типов техник маячка навыков, оставив сам маячок включённым (`skill_beacon_enabled=1`) — в отличие от `quick_toggle_skill`/`toggle_skill_beacon` (bot.py:3035-3036, 4921-4922), которые явно защищают только переход 0→1 самого тумблера от пустого `beacon_types` ("19-й чекап"), эта защита не срабатывает при последующем ручном снятии всех типов уже при включённом маячке. После этого `next_beacon_slot` (bot.py:5060-5079) стабильно возвращает `(None, None)`, и `send_skill_beacon` (bot.py:5278-5292) молча завершается на каждом тике `if slot is None: return` — маячок навсегда немеет без единого сигнала пользователю, пока тот сам не зайдёт обратно в "🎯 Типы маячка".

## 2026-08-29

- `NOTIFY_USER_ID = int(os.getenv("NOTIFY_USER_ID", "0"))` (bot.py:27) по умолчанию равен `0`, а минимум 10 админ-команд (`/newpromo`, `/blogger`, `/grant`, `grant30_callback`, `/users`, рассылки и т.д., bot.py:7597, 7625, 7660, 7678, 7713, 8946, 8972, 9020, 9145, 9205, 9237) авторизуют доступ проверкой `if NOTIFY_USER_ID and uid != NOTIFY_USER_ID: ...⛔ Нет доступа`. Пустой `NOTIFY_USER_ID` (переменная окружения не выставлена при деплое) делает первую часть условия ложной для всех этих команд разом — это не просто "уведомления молча не уходят" (уже отмечено в IDEAS.md как техдолг), а полный обход авторизации: любой пользователь бота получает доступ к выдаче подписок, созданию промокодов и рассылкам.
- `quick_toggle_beacon`/`quick_toggle_skill` (bot.py:3021-3056) — кнопки-переключатели маячков на закреплённом сообщении дня (`daily_prefs_kb`), которое видно весь день, включая всё время прохождения утреннего/вечернего ритуала (пин снимается только в конце, в `finish_evening`→`unpin_today_tasks`). Обе безусловно вызывают `clear_awaiting_flags(ctx, update)` с полным `update` — а этот вызов, по собственному докстрингу функции (bot.py:6443-6450), дополнительно отменяет активный `morning`/`evening` `ConversationHandler`. Это ровно та ловушка, которую уже explicitly обошли в `use_yesterday_plan_callback` (bot.py:2686-2697, комментарий там прямо описывает: полная форма обрывает диалог, кнопки ритуала после этого перестают отвечать, "мягкая" форма `clear_awaiting_flags(ctx)` без `update` используется намеренно) — но тот же фикс не применён к обеим кнопкам маячков на том же самом персистентном сообщении. Тап по маячку вместо ответа на текущий шаг ритуала молча обрывает диалог.
- `evening_notification` (bot.py:8288-8317), плановая рассылка "🌙 Привет! День заканчивается" в 21:00, не проверяет, не идёт ли у пользователя прямо сейчас вечерний ритуал — в отличие от `morning_reminder` (+2ч, bot.py:8778-8805), которая явно вычисляет `morning_conv_active = _morning_conv is not None and (uid, uid) in _morning_conv._conversations` и не шлёт напоминание поверх уже идущего диалога. Ответы вечернего ритуала копятся в `ctx.user_data` и попадают в БД только в `finish_evening`, поэтому для пользователя, начавшего ритуал прямо перед 21:00 и ещё не закончившего его, `get_diary(uid, "evening", today)` всё ещё пуст — плановое уведомление прилетает поверх открытого диалога.
- `schedule_message_deletion`/`sweep_scheduled_deletions` (bot.py:2236-2254, 2400-2414) при истечении TTL молча удаляют сообщение-запрос из чата, но не трогают связанный `awaiting_*`-флаг в `ctx.user_data`. Экраны вроде `_ask_work_start` (`awaiting_work_start`, bot.py:3235-3250), `set_name_prompt`/`set_city_prompt` (`awaiting_name`/`awaiting_city` через `_settings_render`→`_render_tracked`, bot.py:4850-4879, 1436-1456) ставят флаг и планируют самоудаление одним и тем же TTL-механизмом. Если пользователь не ответил вовремя, приглашение исчезает, а флаг остаётся `True` бессрочно — следующее любое произвольное сообщение (хоть через час, хоть на следующий день) молча перехватится как ответ на давно пропавший запрос (например, случайный текст улетит как новое имя или время начала работы) без единого следа на экране, что вообще произошло.
- `handle_edit_reminder_intent` (bot.py:6291-6306) не получил тот же фикс, что и его аналог `handle_delete_reminder_intent` (bot.py:5825-5853, "19-й чекап" — там при нуле совпадений `candidates` больше не подставляет первые попавшиеся напоминания молча, текст честно говорит "не нашёл похожих"). В `handle_edit_reminder_intent` при нуле совпадений код по-прежнему делает `candidates = (matches or items)[:2]` и показывает одинаковое "Не понял, какое именно — выбери, что изменить", не различая реальные совпадения и произвольный фоллбэк из всего списка.
- День 7 research-опроса (`send_research_question`, bot.py:7116-7128) использует callback-значения `"yes"/"maybe"/"no"`, а общая проверка низкой оценки в `research_callback` (bot.py:7201) — `low_rating = value in ("0","1","2") if day==14 else value in ("1","2")` — сравнивает `value` с числовыми строками. Для дня 7 `value` никогда не равно `"1"`/`"2"`, так что даже самый плохой ответ "❌ Нет пока" никогда не ставит `low_rating=True` и не шлёт админу алерт "⚠️ НИЗКАЯ ОЦЕНКА — нужна связь!" — в отличие от дней 3 и 14, где этот же класс бага уже explicitly пофикшен (комментарий "14-й чекап" рядом описывает точно такую же проблему для дня 14).
- `morning_start` в ветке `resuming` (bot.py:2516-2545) при редком крайнем случае, когда все `RESUME_FIELDS` уже есть в `ctx.user_data`, но `morning_filled_at` за сегодня всё же не проставлен (сам код признаёт это в комментарии — `finish_morning` реально не успел отработать), проваливается ниже к блоку "недописанное утро за ПРЕДЫДУЩИЙ день" (2533-2540). Но там условие `stale_date != today_iso` ложно (`stale_date` — это тот же сегодняшний `m_progress_date`), так что `checkpoint_morning_progress` не вызывается, а следующие строки (2542-2545) безусловно затирают `m_writing`/`m_gratitude`/`m_child` через `ctx.user_data.pop`. Уже написанные пользователем свободное письмо/благодарность/слова себе теряются безвозвратно именно в том случае, для защиты от которого checkpoint и был добавлен.
- `finish_morning` (bot.py:3106-3131) хранит мёртвый код: `changed_keys = {k for k in TASK_KEYS if task_fields[k] != existing.get(k, "")}` (3126) призван, по соседнему комментарию, "сбрасывать отметки выполнено только для полей, текст которых реально поменялся" — но `task_fields = _merged_task_fields(ctx, existing)` (3112), а `_merged_task_fields` (3060-3076) по собственному докстрингу теперь буквально возвращает `{key: existing.get(key, "") for key in TASK_KEYS}`, то есть построена из того же `existing`, с которым её потом сравнивают. `changed_keys` гарантированно всегда пустой набор — рудимент до рефакторинга (когда задачи ещё ставились через шаги ритуала M_FOCUS/M_B1/...), который сейчас ничего не делает, а его комментарий вводит в заблуждение о реальном поведении.
- `newpromo_command` (bot.py:7594-7618) подставляет `label` в сообщение с `parse_mode="Markdown"` без экранирования: `who = f", метка «{label}»"` (7617) — соседняя `blogger_command` (7620-7654), работающая с той же концепцией метки, корректно оборачивает её через `md_escape(label)` (7646). Метка с `*`, `_` или `` ` `` в аргументах команды либо ломает форматирование, либо валит `reply_text` с `BadRequest: Can't parse entities` — админ не получает подтверждения о создании кода.
- `grant30_callback` (bot.py:7710-7726, кнопка "🎁 +30" в /users) вызывает `_grant_and_notify` без `try/except`, хотя `grant_command` (7674-7708) специально обзавёлся таким `try/except` вокруг того же вызова с явным комментарием: глобальный `on_error` только пишет в логи, ничего не отвечает в чат, и без локальной обработки сбой (например при отправке уведомления целевому пользователю) выглядит как полное отсутствие реакции на нажатие кнопки.

## 2026-08-30

- `morning_start` (bot.py:2634, 2672) шлёт утреннее приветствие и разминку через один и тот же трекнутый канал `_render_ritual_step`→`track_key="ritual_step"` (bot.py:1512-1518): первый вызов (2634) редактирует `q.message` в `morning_greeting` и запоминает его id, а второй, всего через `asyncio.sleep(0.5)` (2644), находит тот же id и перезаписывает то же сообщение в `warmup_text` вместе с его клавиатурой. Клавиатура приветствия — не декоративная: именно в ней лежит кнопка "✅ Взять как задачи на сегодня" (bot.py:2612) — единственное во всём файле место, откуда генерируется `callback_data="use_yesterday_plan"` (единственный обработчик — `use_yesterday_plan_callback`, зарегистрирован bot.py:9414). Ни один человек не успеет прочитать приветствие и тапнуть кнопку за 0.5 секунды — фактически самая свежая и разрекламированная в CHANGELOG фича ("🆕 Перенос вечернего плана в задачи дня", bot.py:87-92) неработоспособна в проде на каждом обычном утреннем ритуале.
- `handle_delete_pool_intent` (bot.py:5855-5874) не получил тот же фикс "19-го чекапа", что его аналог `handle_delete_reminder_intent` (bot.py:5825-5854): при нуле совпадений (`matches` пуст) код по-прежнему делает `candidates = (matches or items)[:2]` и зовёт `send_pool_delete_menu` с двумя произвольными пунктами пула без какого-либо "не нашёл похожих" — хуже уже задокументированного в BUGS.md 2026-08-29 случая с `handle_edit_reminder_intent` (там хотя бы одинаковый текст переспроса, здесь `send_pool_delete_menu` вообще не различает случаи и просто показывает "Что удалить?").
- Отметка задачи выполненной может незаметно удалить чужой дубликат из «Списка дел»: `finish_evening` (bot.py:3990-3995) и `task_done_callback` (bot.py:6105-6123), если у задачи нет `_pool_link_{key}` (задача напечатана вручную или взята вечерним планом), чистят пул по `for item in get_pool_tasks(uid): if item["text"]...== task_text: delete_pool_task(...)` без `break` — то есть удаляют ВСЕ совпадающие по тексту пункты, а не только связанный с этой задачей. Оба места комментарием ссылаются на "существующую дедупликацию при добавлении дел в пул", но `add_pool_task` (bot.py:867-874) — обычный `INSERT` без какой-либо проверки на дубликаты; у пользователя вполне может быть два независимых пункта с одинаковым текстом (добавил дважды, или вручную и через 📥), и отметка выполненной только одной из связанных с ними задач стирает оба.
- `_edit_tracked_msg` (bot.py:1458-1475, используется как `_edit_settings_msg` в `handle_text`) страдает тем же багом, что уже задокументирован для `_edit_or_send`/`go_tab` (BUGS.md 2026-08-26, п. про "Двойной тап"): голый `except Exception: return False` вокруг `bot.edit_message_text` не отличает `BadRequest: Message is not modified` от реальной ошибки. При повторном одинаковом неверном вводе — время не в формате ЧЧ:ММ дважды подряд (bot.py:6549-6552), пустое имя дважды (6556-6560) или слишком длинное имя дважды (6561-6565) — второй `edit_message_text` падает как no-op, `_edit_tracked_msg` возвращает `False`, и вызывающий код шлёт тот же текст ещё и через `reply_text`, то есть дублирующее сообщение — та же проблема, что уже чинили для кнопок, но для этих конкретных путей ретрая через `handle_text` фикс не применён.
- Для дня 30 research-опроса ("Если бот перестанет работать — как ты к этому отнесёшься?", bot.py:7147-7159) проверка низкой оценки `low_rating = value in ("0","1","2") if day==14 else value in ("1","2")` (bot.py:7201) никогда не срабатывает: callback-значения дня 30 — `"sad"/"meh"/"nope"/"glad"` (bot.py:7153-7158), а не числовые строки. Тот же класс бага, что уже задокументирован для дня 7 (BUGS.md 2026-08-29), но на третьем, ещё не отмеченном месте — самый тревожный сигнал (готовность потерять бота — "😐 Всё равно"/"😢 Очень расстроюсь") никогда не долетает до админ-алерта "⚠️ НИЗКАЯ ОЦЕНКА — нужна связь!".
- `research_awaiting` (БД-флаг, bot.py:7251, 7269, 7277) ничем не ограничен по времени, а сама трекнутая карточка с открытым вопросом отправляется через `send_tracked_notification` без явного `ttl_seconds` (bot.py:7148-7159, 7263-7269, 7271-7277) — то есть самоудаляется по дефолтному `INACTIVE_SCREEN_TTL_SEC` через 15 минут тишины (bot.py:2307-2308), а `sweep_scheduled_deletions` при этом чистит только само сообщение, флаг в БД не трогает. Если пользователь не ответил за 15 минут, карточка с вопросом бесследно исчезает из чата, а `research_awaiting` продолжает висеть неограниченно долго — следующее любое сообщение (хоть через час, хоть на следующий день, на совсем другую тему), если оно не распознаётся `classify_free_text` как более конкретное намерение (bot.py:6806-6813), молча запишется как ответ на давно пропавший вопрос исследования.

## 2026-08-31

- `on_error` (bot.py:9037-9061, новый в #218 "Необработанная ошибка теперь видна пользователю") теперь на любое необработанное исключение отвечает в чат "⚠️ Что-то пошло не так. Попробуй ещё раз или открой меню." — раньше (до этого коммита) такое исключение просто печаталось в лог и было полностью не видно пользователю. Это превратило безобидный двойной тап в ложную тревогу там, где обработчик редактирует сообщение напрямую (`q.message.edit_text(...)`/`ctx.bot.edit_message_text(...)`) без try/except: `day_card_nav` (bot.py:6516-6523) — обе кнопки навигации карточки дня ("◀️ Пред. день"/"След. день ▶️") при быстром двойном тапе шлют два callback'а с ОДИНАКОВЫМ `callback_data` (клавиатура на клиенте не успевает обновиться между тапами), второй `edit_text` падает с `BadRequest: Message is not modified` и теперь всплывает как видимая ошибка; та же незащищённая пара — `quickdisable_field_callback`/`dismiss_nudge_callback` (bot.py:4851-4870), одноразовые кнопки на редко показываемой карточке-подсказке после нескольких пропусков подряд. Импульсивный повторный тап — типичное для аудитории СДВГ поведение, которое остальной файл специально старается не пугать лишним сообщением (см. уже задокументированный класс бага с `_edit_or_send`, BUGS.md 2026-08-26), а здесь новый глобальный хендлер именно это поведение и выдаёт за сбой.
- `log_event(uid, "subscribe_pay_started")` (bot.py:7616) вызывается непосредственно ПЕРЕД `ctx.bot.send_invoice(...)`, у которого нет try/except — та же "запись в БД до успеха действия", которую фиксирующий комментарий у `send_with_privacy_hint` (bot.py:1415-1419) прямо называет уже однажды пофикшенным багом ("16-й чекап": надо писать после успешной отправки, а не до). Если `send_invoice` упадёт (лимиты Stars, временный сбой, неверная конфигурация платежей), в `events` всё равно останется запись "начал оплату" — новая метрика "🛒 Воронка подписки" в `/admin` (bot.py:9218-9223, 9289) в этом случае продолжит показывать нормальную конверсию открыл→начал оплату, скрывая ровно тот сбой, для отлова которого эта воронка и заводилась. `log_event(uid, "subscribe_opened")` в `go_subscribe`/`subscribe_command` (bot.py:7603, 7609) страдает тем же паттерном, но менее критично — там рендер экрана подписки и так падает редко (у `_edit_or_send` есть свой fallback).
- Новая когортная ретеншн-метрика в `/admin` (`_retention`, bot.py:9178-9192, вызывается как `ret1, ret7, ret30`) считает `cohort_date` от `admin_today = datetime.now(get_user_tz(get_user(uid))).date()` — часовой пояс АДМИНА, вызвавшего команду (осознанный выбор, см. комментарий bot.py:9155-9160, чтобы не съезжала граница "7/30 дней" для `diary.date`, которая пишется в личной таймзоне каждого пользователя). Но `cohort_size`/`retained` в `_retention` сравнивают этот `cohort_date` с `users.created_at` — а `created_at` пишется не в личной таймзоне пользователя и не в таймзоне админа, а всегда в таймзоне бота по умолчанию `USER_TIMEZONE` (см. комментарий в `get_user`, bot.py:650-657: "на момент первого /start таймзона пользователя ещё не известна... берём дефолтный пояс бота"). Если личная таймзона админа отличается от `USER_TIMEZONE` (обычное дело — это же ровно то, ради чего у пользователей вообще спрашивают город), `cohort_date` систематически съезжает на день от реальных `created_at`, и когорта может ложно считаться пустой ("— (когорта пуста)") или недосчитывать/задваивать пользователей рядом с полуночью — тот же класс бага, что и комментарий рядом уже описывает, но фикс из этого комментария (взять таймзону админа) для `created_at` как раз и создаёт рассинхрон, а не решает его.

## 2026-09-01

- Новая `apply_yesterday_plan_if_empty` (bot.py:1022-1045, добавлена в #219-#223) вызывается не только из экранов, которые показывают весь день целиком (`show_tasks`, `morning_start`, `morning_task_offer_yes`, `walk_finish_callback`), но и изнутри `apply_task_edit` (bot.py:6026-6045) — общей функции сохранения ОДНОГО слота задачи, которую вызывает в том числе `handle_set_task_intent` (bot.py:5996-6024), обрабатывающий свободный текст вида «поставь задачу — позвонить врачу» (`classify_free_text`, intent `set_task`). Если это первое обращение к задачам за день (пользователь ещё не открывал 📋 Задачи и не заходил в ☀️ Утро) и вчера был вечерний план, `apply_task_edit` через `apply_yesterday_plan_if_empty` молча заполняет ВСЕ шесть слотов вчерашним планом, а не только тот, что пользователь назвал текстом — то есть одна короткая фраза о ОДНОЙ задаче на сегодня незаметно материализует ещё пять вчерашних задач, которые человек не просматривал и не подтверждал. Итоговое подтверждение показывает полный список без пометки, что 5 из 6 пунктов не то, о чём просили — этот конкретный путь (handle_set_task_intent → apply_task_edit на пустой день) не покрыт новыми тестами (`test_tasks_auto_apply_yesterday_plan.py` проверяет только `show_tasks`/`morning_start`).
- Возврат к модели «пункт списка дел не удаляется при выборе, только при отметке ✅» (#223, `pool_use_item` bot.py:6093-6103, `pool_suggestions_kb` bot.py:5817-5840) снял защиту от повторного выбора: `get_pool_tasks(uid)` в `pool_suggestions_kb` не фильтрует пункты, уже привязанные (`_pool_link_{key}`) к другому слоту задач сегодня — один и тот же пункт «Купить молоко» можно выбрать сразу в B1 и в C1, оба слота получат одинаковый текст и оба сошлются на один и тот же `pool_item_id`. До #223 это исключалось самой немедленной вырезкой пункта из пула при первом же выборе; сейчас `task_pool_text`/`pool_suggestions_kb` (bot.py:6124-6135) никак не отмечают «уже используется в задачах дня», и пользователь может не глядя продублировать одно и то же дело на несколько слотов дня.

## 2026-09-02

- `warmup_go` (bot.py:2865-2874) — утренняя разминка после приветствия — крутит цикл `for i, (name, hint) in enumerate(WARMUP): await _edit_tracked_msg(...); await asyncio.sleep(20)`, то есть 6 упражнений × 20 секунд = 120 секунд одной корутиной с шестью `await asyncio.sleep(...)` подряд. `Application.builder()` в `main()` (bot.py:9783-9788) нигде не вызывает `.concurrent_updates(True)` (проверено grep по всему файлу — совпадений нет), а значит действует дефолт python-telegram-bot v20 — апдейты обрабатываются строго последовательно, следующий апдейт (от ЛЮБОГО пользователя) не забирается из очереди, пока не завершится корутина текущего хендлера целиком, включая все её `await`. То есть каждое обычное прохождение утренней разминки — самое частое ежедневное действие в боте — на 2 минуты останавливает обработку сообщений и колбэков вообще для всех пользователей бота одновременно, причём именно в утренние часы, когда активность синхронизирована сильнее всего. Это не то же самое, что уже задокументированная блокировка event loop синхронными вызовами Anthropic (BUGS.md 2026-08-27/08-30) — там блокировка случайная и зависит от сети, здесь она гарантированная, детерминированная и встроена в самый обычный путь использования бота.
- `get_today_context` (bot.py:1317-1326, `today, morning, done_set = ...`) не вызывает `apply_yesterday_plan_if_empty` (bot.py:1039), в отличие от `show_tasks`/`morning_start`/`handle_set_task_intent`/`walk_finish_callback`, уже перечисленных в IDEAS.md 2026-09-01 как единственные места с этим бэкфиллом. Но `get_today_context` — общий хелпер, вызываемый в 10 местах: `send_coach` (4463), `beacon_technique_done` (5347), `send_task_beacon` (5425), `send_work_start_reminder` (5592), `go_focus` (7219), `focus_start_callback` (7287), `buddy_ping` (8258), `send_resume_check` (8277), `midday_notification` (8305) и `midday_callback` (8355) — он читает `get_diary(uid,"morning",today)` напрямую, минуя бэкфилл. Если первое обращение пользователя к боту за день — это плановый ☕ дневной чекин в 13:00 (самая частая проактивная точка входа в файле) или открытие 🍅 Фокус-режима, `midday_notification`/`go_focus` увидят пустой `morning={}` и покажут ветку «утренний дневник не заполнен» с урезанной клавиатурой, хотя реальный вчерашний план есть и был бы автоматически подставлен, открой пользователь вместо этого 📋 Задачи. Один и тот же день выглядит по-разному в зависимости от того, какой экран открылся первым.
- `get_access_status`/`get_trial_days_left` (bot.py:697-728) страдают тем же классом рассинхрона таймзон, что уже задокументирован для ретеншн-когорты в `/admin` (BUGS.md 2026-08-31), но здесь дефект бьёт по самому пейволлу, а не по дашборду. `today = datetime.now(get_user_tz(user)).date()` вычисляется в личной таймзоне пользователя (уже известной на момент проверки доступа), а `created_date` берётся из `created_at`, который по комментарию в `get_user` (bot.py:660-667) навсегда пишется в таймзоне бота по умолчанию `USER_TIMEZONE` в момент первого `/start`, ещё до того как пользователь указал город, и никогда не пересчитывается. Если личная таймзона пользователя заметно отличается от `USER_TIMEZONE` (обычный случай — ради этого различия и спрашивают город), `trial_end = created_date + timedelta(days=TRIAL_DAYS + extra_days)` систематически съезжает на день от того, что пользователь считает днём регистрации по своим часам — жёсткий пейволл `access_gate` может сработать на день раньше или позже ожидаемого, а показанное число оставшихся дней пробного периода (`get_trial_days_left`) — быть неверным.
- `_reply_chunked_markdown` (bot.py:9363-9383) — новый общий хелпер, специально сделанный, чтобы не резать Markdown-разметку посередине пары `*...*` (фикс старого бага о сырой нарезке `msg[i:i+4000]`) — режет только МЕЖДУ уже готовыми `part`-ами, но не проверяет, что сам по себе `part` не превышает `limit`. Если единственный `part` (`f"*{name}* ({date_str}):\n{md_escape(text)}"`, собирается в `admin_feedback` bot.py:9401 и `admin_research` bot.py:9427) уже длиннее 4000 символов — что реально при насыщенном спецсимволами длинном фидбеке пользователя, так как `md_escape` (экранирует `_`, `*`, `` ` ``, `[`) почти удваивает длину текста с такими символами — код всё равно кладёт его целиком в `chunk` (условие `if chunk and len(candidate) > limit` ложно, пока `chunk` пуст) и в итоге шлёт этот `chunk` через `message.reply_text(chunk, parse_mode="Markdown")`, где он может превысить и жёсткий лимит Telegram в 4096 символов. Ни `admin_feedback`, ни `admin_research` не оборачивают вызов `_reply_chunked_markdown` в try/except — падение будет необработанным исключением, видимым только в логах (или всплывёт админу как «⚠️ Что-то пошло не так» через новый `on_error`), без единого содержательного ответа на команду.
- `buddy_ping` (bot.py:8253-8267, кнопка «💬 Написать бадди сейчас» на персистентном трекнутом сообщении канала `"buddy"`) — единственный обработчик на этом экране, не вызывающий `clear_awaiting_flags(ctx, update)`: соседние `buddy_menu`/`buddy_set` его вызывают, `buddy_ping` — нет. Тот же класс уже описанного бага (BUGS.md 2026-08-29, про `quick_toggle_beacon`/`quick_toggle_skill` на persistent-сообщениях), но в новом, ранее не перечисленном месте. Сценарий: сообщение с шаблоном бадди может оставаться в чате, пока пользователь параллельно открывает другой экран (например «✏️ Изменить имя», ставящий `awaiting_name=True`); вернувшись к старому сообщению бадди и нажав «Написать бадди сейчас», пользователь не сбросит `awaiting_name` — следующее любое обычное сообщение молча утечёт в обработчик смены имени.
- «🎁 Промокод» (`go_promo`, bot.py:7933) зарегистрирован как обработчик (`CallbackQueryHandler(go_promo, pattern="^go_promo$")`, bot.py:9976) и явно упомянут в `ACCESS_GATE_EXEMPT_CALLBACKS` (bot.py:8148), но нигде во всём файле ни одна `InlineKeyboardButton` не эмитит `callback_data="go_promo"` (подтверждено grep) — ни на экране подписки (`_subscribe_text_and_kb`), ни в главном меню, ни на пейволле. Единственный способ добраться до ввода промокода — вручную набрать команду `/promo`, о существовании которой в интерфейсе нигде не сказано, если пользователю не прислали код текстом отдельно (например, блогерский промокод из `/blogger`). Комментарий в самом коде (bot.py:7958: «Нажатие "🎁 Промокод" (awaiting_promo_code)») написан так, будто кнопка есть — похоже на регрессию (кнопку убрали/не добавили при рефакторинге меню), а не осознанное решение. Особенно чувствительно для пользователя с истёкшим пробным периодом: промокод — единственный бесплатный путь продлить доступ, и рядом с пейволлом (bot.py:8152-8214) о нём нет ни слова.
- `pool_change_page` (bot.py:6000-6014) и `evening_plan_show_more` (bot.py:4033-4037) вызывают `q.message.edit_reply_markup(...)` напрямую, без try/except и без общих примитивов `_edit_msg_or_send`/`_edit_tracked_msg` (bot.py:1507-1553), которые уже умеют отличать безобидный `BadRequest: Message is not modified` от настоящей ошибки. Тот же класс бага, что уже чинили для текстовых экранов (BUGS.md 2026-08-26/08-30/08-31), но для `edit_reply_markup` он не пофикшен ни разу: повторный тап по той же стрелке "◀️"/"▶️" пагинации пула или плана на завтра (типичный импульсивный повторный тап) вычисляет идентичную клавиатуру той же страницы — второй `edit_reply_markup` падает как no-op-ошибка, никем не перехваченной, и всплывает пользователю через новый `on_error` (BUGS.md 2026-08-31) как «⚠️ Что-то пошло не так».
- `beacon_technique_done` (bot.py:5325-5339) и `task_done_callback` (bot.py:6377-6382) — оба вызывают `clear_awaiting_flags(ctx, update)` в полной форме, которая по докстрингу самой функции (bot.py:6757-6772) не только гасит `awaiting_*`-флаги, но и отменяет активный `_morning_conv`/`_evening_conv` для пользователя. Это тот же класс бага, что уже пофикшен для `quick_toggle_beacon`/`quick_toggle_skill` (BUGS.md 2026-08-29, «16-й чекап», там используется мягкая форма `clear_awaiting_flags(ctx)` без `update`) — но фикс не добрался до этих двух функций. Обе кнопки живут на сообщениях, приходящих независимо от активного ритуала: маячок навыков — от фонового планировщика, чекбокс задачи — на 📋 Задачи, на маячке задач, на дневном чекине и на resume-check. `task_done_callback` — один из самых часто нажимаемых колбэков в файле; тап по чекбоксу задачи (например, походя отметить вчерашнюю задачу выполненной) во время открытого шага утреннего/вечернего ритуала молча обрывает диалог — следующий ответ на вопрос ритуала перестаёт обрабатываться как его часть.
- TTL-самоудаление рвёт связь с `awaiting_*`-флагом (тот же класс бага, что и в BUGS.md 2026-08-29 про `awaiting_work_start`/`awaiting_name`/`awaiting_city`) ещё в четырёх местах, не упомянутых там как примеры: `awaiting_task_edit` (`_offer_task_input` bot.py:5759-5770, `ask_task_text` bot.py:5948-5957), `awaiting_pool_add` (`pool_add_start` bot.py:6332-6339), `awaiting_reminder_add` (`reminder_add_start` bot.py:6537-6546) и `awaiting_reminder_edit` (`ask_reminder_edit` bot.py:6560-6574) — все ставят флаг и планируют самоудаление приглашения через тот же `ttl_seconds=INACTIVE_SCREEN_TTL_SEC` (15 минут), а `handle_text` (bot.py:7012-7043) читает эти флаги без проверки давности. Для напоминаний последствие хуже, чем в уже описанных случаях: если `parse_reminder_request` не смог разобрать текст, код сам себя переустанавливает — `ctx.user_data["awaiting_reminder_add"] = True` (bot.py:7027) / `ctx.user_data["awaiting_reminder_edit"] = rem_id` (bot.py:7039) — без какой-либо проверки, сколько времени уже прошло. Получается самоподдерживающаяся ловушка: приглашение исчезло из чата 15+ минут назад, а флаг не только остаётся висеть, но и переустанавливает себя на каждое следующее сообщение, которое не удалось разобрать как время/дату — то есть на любую случайную реплику пользователя бот продолжает отвечать «Не получилось понять, когда напомнить», пока пользователь случайно не напишет что-то, что AI ошибочно распознает как дату/время.
- Пейволл `access_gate` (bot.py:8152-8214) — единственное место в файле, отвечающее на действие пользователя обычным `target.reply_text(paywall_text, ...)` (bot.py:8210-8212) без какого-либо трекинга предыдущего такого сообщения, в отличие от всех остальных `send_tracked_notification`/`_render_tracked`-based экранов, которые существуют именно для того, чтобы повторные действия не плодили сообщения подряд (см. IDEAS.md 2026-08-27 про 4 параллельных механизма «одно сообщение на канал» — `access_gate` не участвует ни в одном из них). `access_gate` срабатывает на КАЖДОЕ заблокированное действие пользователя с истёкшим доступом, а это ровно тот случай, где импульсивные повторные тапы (типичное поведение аудитории с СДВГ, что явно признаётся в других частях файла) наиболее вероятны: пользователь тыкает 📋 Задачи, потом 🍅 Фокус, потом 🧠 Навыки один за другим, не понимая, почему ничего не открывается, и получает 3+ идентичных сообщения с пейволлом подряд.

## 2026-09-04

- `get_today_context` (bot.py:1331-1341, чинили в #242 ровно для того, чтобы разные экраны не показывали то пустой день, то подтянутый план в зависимости от точки входа) теперь вызывает `apply_yesterday_plan_if_empty` (bot.py:1053-1075), которая при первом же обращении к пустому дню молча пишет в БД (`save_diary`) все 6 слотов вчерашнего вечернего плана. Но `get_today_context` вызывается не только из экранов, открытых пользователем, а ещё из четырёх ФОНОВЫХ функций планировщика, срабатывающих раз в минуту сами по себе, без единого действия пользователя: `send_task_beacon` (bot.py:5432, вызов на 5496), `send_work_start_reminder` (bot.py:5653, вызов на 5670), `send_resume_check` (bot.py:8429, вызов на 8437), `midday_notification` (bot.py:8460, вызов на 8469). Раньше (до #242) эти четыре места читали `get_diary(uid,"morning",today)` напрямую, без побочных эффектов — сейчас же самый обычный фоновый тик (например, дневной маячок задач просто проверяет, пора ли напомнить) может, никак не спросив пользователя и до того, как он вообще открыл бота сегодня, материализовать в БД полный список из 6 вчерашних задач как «сегодняшний план». Если человек специально хотел начать день с чистого листа (плохое самочувствие, планы поменялись) и планировал сам решить это позже через ☀️ Утро, у него это решение фоновой тик может отобрать раньше, чем он вообще откроет бота.
- Утренняя разминка (`warmup_go`, bot.py:2916-2925) с фикса #241 зарегистрирована с `block=False` (bot.py:10061), чтобы её собственные 6×20с `asyncio.sleep` не блокировали весь бот целиком (что и было нужно) — но `ConversationHandler.check_update` (python-telegram-bot 20.7, `_conversationhandler.py`) на время выполнения такого фонового таска переводит разговор в `PendingState` и на этот период проверяет входящие апдейты ТОЛЬКО по хендлерам состояния `ConversationHandler.WAITING`, которого нигде во всём файле не существует (проверено — ни один хендлер не зарегистрирован для `states={ConversationHandler.WAITING: [...]}`). Значит, пока разминка ещё крутится (до ~2 минут), тап по видимой на экране кнопке «Пропустить →» (`skip_warmup`, callback зарегистрирован только внутри состояния `M_EXERCISE`, bot.py:2933, 10063) не совпадает ни с одним хендлером, `check_update` возвращает `None`, апдейт просто отбрасывается насовсем (не откладывается на потом) — колбэк никогда не отвечается (`q.answer()` не вызывается), кнопка в клиенте Telegram остаётся в состоянии загрузки, а сама разминка продолжает идти все отведённые 2 минуты как ни в чём не бывало. Тот же эффект — у повторного тапа по самой `warmup_go` (двойной тап). Регрессионный тест `test_warmup_nonblocking.py` сам явно оговаривает, что проверяет только неблокировку ДРУГИХ пользователей, а не этот сценарий («not a re-simulation of PTB's own... internals»).
- Новый общий хелпер `_awaiting_flag_expired` (bot.py:6898, фикс #249 для бага из BUGS.md 2026-09-02 про протухающие `awaiting_*`-флаги) подключён только к четырём флагам, у каждого из которых заведена отдельная метка `f"{flag}_set_at"`: `awaiting_task_edit`, `awaiting_pool_add`, `awaiting_reminder_add`, `awaiting_reminder_edit`. Но самый первый экземпляр именно этого класса бага, из-за которого он вообще был описан (BUGS.md 2026-08-29), — это `awaiting_work_start` (`_ask_work_start`, bot.py:3439-3454: ставит флаг и планирует самоудаление приглашения тем же `INACTIVE_SCREEN_TTL_SEC` через `schedule_message_deletion`, но не заводит `_set_at`), а также `awaiting_name`/`awaiting_city` (`set_name_prompt`/`set_city_prompt`, bot.py:5113-5142, самоудаляются через `_settings_render`→`_render_tracked` с `SETTINGS_MENU_TTL_SEC`). Ни один из этих трёх флагов, изначально названных в первом описании бага, до сих пор не получил `_set_at` и не проверяется в `handle_text` через `_awaiting_flag_expired` — как и `awaiting_buddy`/`awaiting_feedback`/`awaiting_promo_code`. Баг из BUGS.md 2026-08-29 исправлен только частично: ровно для тех флагов, для которых он изначально был замечен, ловушка «приглашение исчезло из чата, флаг остался висеть навсегда» по-прежнему воспроизводится.
- Фикс #250 для спама пейволлом (`access_gate`, bot.py:8300-8373) заменил `target.reply_text(...)` на трекнутое сообщение под единым ключом `"paywall"` (`_edit_tracked_msg`/`_render_tracked`, bot.py:1591, 1569) — это решает исходный сценарий из BUGS.md 2026-09-02 (повторные попытки в ОДНОМ и том же месте чата), но не решает описанный в том же баге сценарий «тыкает 📋 Задачи, потом 🍅 Фокус, потом 🧠 Навыки один за другим»: `_edit_tracked_msg(ctx, "paywall", ...)` при повторном заблокированном действии редактирует message_id, запомненный от ПЕРВОГО показанного пейволла, а не `target = update.effective_message` (сообщение с только что нажатой кнопкой на другом экране). Если второе заблокированное действие — тап по кнопке на другом сообщении в чате (не там, где пейволл появился впервые), пользователь не увидит у места своего тапа вообще никакой реакции (кроме того, что у кнопки перестаёт крутиться спиннер — `q.answer()` вызывается отдельно) — фактический ответ «Пробный период закончился» тихо обновится в другом, возможно давно прокрученном вверх сообщении. `test_paywall_tracked.py` проверяет только повторные ТЕКСТОВЫЕ сообщения (где `target` — всегда неотредактируемое сообщение пользователя, и `_render_tracked` поэтому всегда правит одно и то же отслеживаемое сообщение бота); сценарий с колбэком на другом экране тестом не покрыт.

## 2026-09-05

- `_render_step_msg`/`_render_ritual_step` (bot.py:1618-1679, 1686-1692, общий `track_key="ritual_step"`) — один и тот же трекнутый слот для ВСЕХ шагов ритуала, разделяемый утренним и вечерним флоу без каких-либо различий: `morning_start`/`evening_start` (bot.py:2673, 3590) оба рендерят свои экраны исключительно через `_render_ritual_step`, а единственное место, которое чистит `ritual_step_msg_id`/`_chat_id` — `_finish_ritual_cleanup` (bot.py:2638-2670) — вызывается только из `finish_morning`/`finish_evening`, а не из самих `morning_start`/`evening_start` при входе. Кнопки «☀️ Утро» и «🌙 Вечер» показаны в одном и том же ряду одной клавиатуры (bot.py:1730-1731), которая переотправляется очень часто (главное меню, вкладка «Сегодня»), поэтому в чате легко скапливается несколько её визуально одинаковых копий. Если пользователь жмёт «☀️ Утро» на одной копии (то же сообщение редактируется на месте в разминку, id запоминается как `ritual_step_msg_id`), а в пределах `STEP_MSG_STALE_SEC` (5 минут, bot.py:1616) успевает нажать «🌙 Вечер» на ДРУГОЙ, ещё не «протухшей» копии того же меню — `evening_start` (bot.py:3690: `_render_ritual_step(q.message, ctx, evening_greeting, parse_mode="Markdown")`, без `reply_markup`) правит не только что нажатое сообщение, а старое, всё ещё «свежее» по `ritual_step_msg_ts` — то есть уже морфнутое утром сообщение с разминкой. Поскольку `reply_markup` в вызове не передан, `_edit_tracked_msg` (bot.py:1591-1614, `bot.edit_message_text(...)` без `reply_markup`) не трогает клавиатуру Telegram — на новом тексте вечернего приветствия остаются старые кнопки разминки («▶️ Начать разминку»/«Пропустить»). Сообщение, на которое реально нажал пользователь, остаётся нетронутым и больше ни на что не отвечает, а вечерний ритуал продолжает вестись в чужом, не совпадающем с местом тапа сообщении.
- `go_menu`/`menu_command`/`go_menu_more` (bot.py:6928-6932, 6934-6942, 6944-6949; регистрация bot.py:10151-10152, 10219) — обработчики самой частой во всём файле кнопки «◀️ Меню» и нового `/menu` (#252) — вызывают `clear_awaiting_flags(ctx, update)` в ПОЛНОЙ форме. Как прямо описывает докстринг самой функции (bot.py:6856-6863), полная форма дополнительно отменяет активный `_morning_conv`/`_evening_conv` для пользователя (bot.py:6891-6896) — осознанный фикс для случая «ушёл в другой раздел ПОСРЕДИ ритуала, текст утёк не туда» (тот же класс уже задокументирован в BUGS.md 2026-08-29 для `quick_toggle_beacon`/`quick_toggle_skill`, где вместо этого умышленно используется мягкая форма без `update`, и расширен в 2026-09-02 на `beacon_technique_done`/`task_done_callback`). Но «◀️ Меню» есть на 30+ статичных, ничего в данный момент не спрашивающих экранах по всему файлу (Коуч, Навыки, Настройки и т.д., включая персистентное дневное сообщение) — если пользователь, находясь посреди утреннего/вечернего ритуала, импульсивно тапнет «◀️ Меню» на одном из ЭТИХ давно открытых экранов вместо ответа на текущий шаг ритуала, `go_menu` молча отменяет диалог. Активный шаг (например «🙏 Благодарность», ждущий свободный текст) остаётся на экране с рабочей на вид кнопкой «Пропустить →», но conversation для пользователя уже не существует: следующий текстовый ответ пролетает мимо `got_gratitude` (зарегистрирован только внутри состояния `M_GRATITUDE`) в общий `handle_text` и не сохраняется в дневник, а тап по «Пропустить →» вообще не находит ни одного обработчика вне активного состояния и просто не отвечает. Тот же класс проблемы, что уже дважды чинили точечно для двух конкретных кнопок на одном персистентном сообщении — но самая частая кнопка в файле, присутствующая на наибольшем числе экранов, этот фикс так и не получила.
- `_walk_to_step` (bot.py:5899-5925) ставит `ctx.user_data["task_walk"] = True` (bot.py:5923), когда очередной слот последовательного прохода пуст — но, в отличие от `awaiting_task_edit` (получил TTL-проверку `_awaiting_flag_expired` в #249, см. BUGS.md 2026-09-04), у `task_walk` нет ни `_set_at`, ни какой-либо проверки давности: единственные места, которые его вообще снимают — безусловный `ctx.user_data.pop("task_walk", None)` внутри `clear_awaiting_flags(ctx, update)` (bot.py:6872, полная форма, только при явной навигации) и `apply_task_edit` (bot.py:6340, только когда слот реально сохраняют). Если пользователь начал проход («✏️ Поставить/изменить задачи»), уткнулся в пустой слот и просто пропал из чата, `task_walk=True` виснет в персистентном (`PicklePersistence`) `ctx.user_data` бессрочно. Это тихо ломает сразу три независимых фоновых уведомления: `send_task_beacon` (bot.py:5448), `send_skill_beacon` (bot.py:5602) и `midday_notification` (bot.py:8466) — все три первым делом проверяют `task_walk` и молча выходят, если он True, так что маячки задач/навыков и дневной чекин в 13:00 перестают приходить этому пользователю навсегда, без единого объяснения на экране. Хуже того, `apply_task_edit` (bot.py:6340) безусловно проверяет тот же `task_walk` при СЛЕДУЮЩЕМ сохранении любого слота — в том числе через `handle_set_task_intent` (bot.py:6246-6274), обработчик свободного текста вида «поставь задачу — …», который сам существует именно чтобы не заставлять «топтаться по шагам» (докстринг, bot.py:6249-6250). Пользователь, который через день или неделю просто пишет боту одну короткую фразу про одну задачу, вместо подтверждения получает продолжение давно забытого прохода — бот сам открывает следующий пустой слот и просит его заполнить, как будто проход всё ещё идёт.

## 2026-09-06

- `coworking_join_callback` (bot.py:7861-7885) игнорирует возвращаемое значение `join_coworking_session` (bot.py:1227-1242) — функция явно документирована как «True, если это НОВОЕ присоединение... False, если уже был в сессии», специально чтобы решать, нужно ли обновлять счётчик у уже присоединившихся. Вызов на bot.py:7869 результат никак не проверяет, и код безусловно шлёт `send_tracked_notification` КАЖДОМУ участнику сессии на каждый вызов колбэка — в том числе на повторный/двойной тап «Присоединиться» тем же пользователем, для которого счётчик участников не изменился. Импульсивный повторный тап (типичное для аудитории с СДВГ поведение, которое остальной файл специально старается не провоцировать лишними сообщениями) на групповой сессии коворкинга рассылает лишний пуш всем остальным участникам с тем же самым, не изменившимся числом.
- `coworking_join_callback` (bot.py:7861-7885) — единственный из колбэков на активных/персистентных сообщениях в этой фиче, не вызывающий `clear_awaiting_and_cancel_ritual` ни в мягкой, ни в полной форме, — в отличие от уже описанных похожих случаев на фоновых уведомлениях (`buddy_ping`, BUGS.md 2026-09-02). Если у пользователя параллельно открыт другой экран с `awaiting_*`-флагом (например «✏️ Изменить имя»), тап «Присоединиться» на карточке коворкинга этот флаг не снимет — следующее обычное сообщение молча утечёт в обработчик, ожидающий совсем другого ввода.
- `awaiting_time` (устанавливается в `set_time_prompt`, bot.py:5733-5755; читается в `handle_text`, bot.py:8174-8225) — единственный из «старых» awaiting-флагов настроек времени уведомлений, который так и не получил `_set_at`/проверку через `_awaiting_flag_expired`, хотя его прямой сосед по тому же `handle_text` — новый `awaiting_coworking_time` (bot.py:7831-7832, 8369-8397) — эту защиту получил сразу при создании. Приглашение «Введи время для ... уведомления» самоудаляется по `SETTINGS_MENU_TTL_SEC`, а флаг остаётся висеть бессрочно: следующее случайное сообщение, совпавшее с форматом ЧЧ:ММ (например, отправленное совсем по другому поводу), молча перезапишет время выбранного уведомления. Тот же класс уже частично пофикшенного бага (BUGS.md 2026-08-29/2026-09-04), но конкретно этот флаг ни разу не был назван в списке недочиненных.
- `_carry_unfinished_tasks_to_pool` (bot.py:4040-4061) пропускает перенос невыполненной задачи обратно в пул, если у неё стоит `_pool_link_{key}` — по комментарию функции (bot.py:4044-4046), предполагая, что связанный пункт пула «и так всё ещё лежит в пуле нетронутым (удаляется только при отметке ✅)». Но `pool_delete_item` (bot.py:7324-7329, кнопка «🗑» в 📥 Список дел) удаляет любой пункт пула по id безусловно, никак не сверяясь с `_pool_link_*` активных задач дня. Если пользователь выбрал пункт пула для задачи (`pool_use_item`), а затем в тот же день вручную удалил тот же пункт из списка дел (посчитав его дублем/лишним), саму задачу так и не выполнив — при вечернем закрытии дня `_carry_unfinished_tasks_to_pool` видит `_pool_link_{key}` и молча НЕ возвращает текст задачи в пул, хотя пункта там уже нет. Невыполненная задача теряется безвозвратно — ровно тот сценарий, для защиты от которого функция и была написана.
- Реактивационное сообщение после долгого перерыва (`send_reengagement_message`, вехи `REENGAGE_MILESTONES=(3,7,14)` от `_days_since_seen`/`last_seen_at`, отправляется в `_process_user_notifications` при `9<=час<10`, bot.py:10804, 11358-11380) может прийти в тот же день и в то же часовое окно, что и ветка «👋 С возвращением!» в `morning_notification` (`is_returning = _days_since_last_activity(uid) >= WELCOME_BACK_GAP_DAYS(3)`, bot.py:10572-10586, 10634-10659) — она шлётся, как только `now>=notif_morning` (по умолчанию «09:00», то же окно). Для полностью пропавшего пользователя (не открывал бота и не завершал ни одного ритуала несколько дней подряд) оба разрыва — по стрику и по `last_seen_at` — обычно достигают одного и того же порога (3/7/14) в один и тот же день: пользователь получает подряд два разных, по-разному сформулированных «ты давно не был(а)» сообщения с разными наборами кнопок (`go_morning`/`go_tasks` против `reengage_start`/`reengage_adjust`/`reengage_leave`) вместо одного связного.
- Калибровка фокус-таймера «Думал(а) X мин — по факту прошло Y мин» (`task_done_callback`, bot.py:7452-7471) сбрасывает `focus_task_text`/`focus_task_estimate_minutes`/`focus_task_started_at` только когда отмеченная выполненной задача совпадает текстом с последней отслеженной фокус-раундом (bot.py:7456-7457, 7471). Но кнопка «🎉 Все задачи сделаны» дневного чекина (`mid_all_done`, bot.py:1694, 10341-10343) отмечает все задачи разом через `mark_tasks_done` (bot.py:1645-1651) в обход `task_done_callback` целиком — этот путь никогда не сбрасывает `focus_task_*`. Если задача с тем же текстом (например, часто повторяющееся «Ответить на почту») в другой день будет отмечена выполненной через этот путь без свежего фокус-раунда над ней, при следующем реальном срабатывании калибровки код покажет заметку с бессмысленным Y — временем, прошедшим с давно забытого `focus_task_started_at`, а не с реального начала работы.

## 2026-09-07

- Новый экран статуса коворкинг-сессии (`_coworking_session_kb`, bot.py:7945-7949 — единственное место с кнопкой «🚪 Не смогу») отправляется через `send_tracked_notification` без `ttl_seconds` (создание — `coworking_set_duration` bot.py:8011-8014, обновление счётчика при чужом присоединении/выходе — `coworking_join_callback`/`coworking_leave_callback` bot.py:8088-8091, 8120-8123), то есть с дефолтным `INACTIVE_SCREEN_TTL_SEC` = 15 минут тишины — тем же классом бага, что уже задокументирован для `focus_timer` (BUGS.md 2026-08-27), только там дело было в раунде на 25-90 минут, а сессию коворкинга с этой же ночи (#300, `IDEAS.md` 2026-09-06 «можно запланировать на завтра») можно создать на много часов вперёд («завтра 09:00»). Если за 15 минут после создания/последнего обновления никто не взаимодействует (обычный случай для сессии, запланированной на завтра — создатель просто закрывает бота до утра), это единственное сообщение — а с ним и единственная во всём боте кнопка «🚪 Не смогу» для этой сессии — молча исчезает из чата на много часов раньше, чем сессия вообще начнётся. Восстановить его неоткуда: список открытых сессий (`go_coworking`, bot.py:7951-7978) для уже присоединённой сессии всё равно показывает «Присоединиться» (нет отдельного состояния «ты уже здесь»), а `coworking_join_callback` (bot.py:8075-8078) на такой тап просто отвечает всплывающим «Ты уже в этой сессии» и завершается без единого перерендера экрана — клавиатура с «🚪 Не смогу» нигде больше не появляется. Участник (в том числе создатель) на много часов теряет любой способ выйти из уже созданной сессии через интерфейс бота. Ни `test_coworking_leave.py`, ни `test_coworking_schedule_ahead.py` не проверяют `ttl_seconds` этих сообщений.
- Дедуп «реактивация vs обычное утреннее уведомление» (#296, `_process_user_notifications`, bot.py:11642-11654, 11720+) вычисляет `reengage_due_now` только в окне `9 <= now_dt.hour < 10` и этим же флагом гасит на сегодня только ветку обычного утреннего уведомления (`if (... and not reengage_due_now): ... morning_notification(...)`, bot.py:11651-11658). Но у пользователя с `notif_morning`, вручную выставленным раньше 9:00 (обычная настройка через ⚙️ Общие → время уведомлений, ничем не ограничена снизу — `set_time_prompt`/`handle_text`, bot.py:5865+, 8448+), утреннее уведомление (со своей независимой веткой «👋 С возвращением!», `is_returning`/`WELCOME_BACK_GAP_DAYS`, bot.py:10946-10978) успевает уйти и пометить `morning_sent_date=day_key` ещё ДО открытия окна 9:00-10:00 — а сам `reengage_due_now`, вычисленный позже в том же тике внутри окна, гасит уже нечего (условие с ним стоит только у ещё не отправленного утреннего). Реактивационное сообщение по `REENGAGE_MILESTONES` (bot.py:11720+) всё равно уходит следующим, ничем не сдержанное второй частью фикса — на вехах 3/7/14 дня пользователь с ранней настройкой утра получает подряд два разных «ты давно не был(а)» сообщения в один день, то есть ровно тот баг, который #296 должен был устранить целиком. Новый регрессионный тест `test_reengage_welcome_back_dedup.py` проверяет только `notif_morning="09:00"` (ровно на границе окна) и не покрывает более раннее время.

## 2026-09-08

- `_send_backup_email_sync` (bot.py:12137-12153, добавлена в #311 для дублирования бэкапа БД на почту) открывает `smtplib.SMTP_SSL("smtp.gmail.com", 465)` (bot.py:12151) без параметра `timeout` — при зависании соединения с Gmail (сетевой сбой на хостинге, временная недоступность SMTP) вызов заблокируется на неопределённое время. Функция запускается через `await asyncio.to_thread(_send_backup_email_sync, ...)` (bot.py:12206), то есть занимает один из воркеров дефолтного `ThreadPoolExecutor` — того же пула, которым делят все остальные блокирующие вызовы бота: снимок БД `_do_backup` (bot.py:12196), синхронные вызовы Anthropic (`asyncio.to_thread`, bot.py:4952,5016,5128,5154,5213) и геокодинг города/таймзоны через `loop.run_in_executor(None, _sync_tz_lookup, city)` (bot.py:1501). Ни один custom executor нигде не задаётся (`Application.builder()`/`asyncio` — проверено grep, `ThreadPoolExecutor`/`set_default_executor` в файле не встречаются), так что все они действительно делят один и тот же пул с ограниченным числом воркеров. Ежедневная плановая джоба бэкапа (и любой ручной `/backup` поверх неё) при зависшем SMTP тихо съедает воркер навсегда; при повторных зависаниях (несколько дней подряд без перезапуска процесса) пул может исчерпаться, и все остальные to_thread-вызовы — в первую очередь AI-коуч и разбор напоминаний — начнут просто не отвечать без единой видимой ошибки. Существующий `tests/test_backup_email.py` подменяет `smtplib.SMTP_SSL` полностью управляемым фейком и не может поймать этот сценарий зависания.
